import {
    TaskManager,
    ThemeProvider,
} from "@schilling-apps/schilling-widgets-system";

// This is just a test to verify the imports work
console.log("TaskManager:", TaskManager);
console.log("Task type available");
console.log("ThemeProvider:", ThemeProvider);
